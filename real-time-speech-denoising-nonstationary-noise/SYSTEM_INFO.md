# System Information (Reproducibility)

Fill this out with the exact machine used for experiments.

- CPU:
- OS:
- Python:
- NumPy:
- SciPy:

## BLAS backend
BLAS is an interface; please record the implementation used (e.g., Intel MKL, OpenBLAS).

How to obtain:
- MATLAB: `version -blas`
- Python/NumPy: `python -c "import numpy as np; np.__config__.show()"`
