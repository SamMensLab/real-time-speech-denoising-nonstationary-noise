# Data Instructions

This repository does not redistribute copyrighted datasets.

To run evaluation, place paired WAV files as:

- `data/test_noisy/`  (noisy input)
- `data/test_clean/`  (clean reference)

Pairing is by identical filename. Sample rate should match the config (default 16 kHz).
